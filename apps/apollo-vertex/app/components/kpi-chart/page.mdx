import { KpiChartTemplate } from '@/templates/charts/KpiChartTemplate';

# KPI Chart

A single scalar metric (count, sum, average, min, max).

<div className="not-prose my-8">
  <KpiChartTemplate />
</div>

## Installation

```bash
npx shadcn@latest add @uipath/kpi-chart
```
