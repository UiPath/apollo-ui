import { Field, FieldContent, FieldDescription, FieldLabel } from '@uipath/apollo-vertex'
import { Input } from '@uipath/apollo-vertex'
import { Textarea } from '@uipath/apollo-vertex'

# Field

A form field component with label, input, and description.

<div className="p-4 border rounded-lg mt-4 space-y-6">
  <Field>
    <FieldLabel>Email</FieldLabel>
    <FieldContent>
      <Input type="email" placeholder="Enter your email" />
      <FieldDescription>We'll never share your email.</FieldDescription>
    </FieldContent>
  </Field>

  <Field>
    <FieldLabel>Bio</FieldLabel>
    <FieldContent>
      <Textarea placeholder="Tell us about yourself" />
      <FieldDescription>Max 500 characters.</FieldDescription>
    </FieldContent>
  </Field>
</div>

## Installation

```bash
npx shadcn@latest add @uipath/field
```

## Usage

```tsx
import {
  Field,
  FieldContent,
  FieldDescription,
  FieldLabel,
} from "@uipath/apollo-vertex"
```

```tsx
<Field>
  <FieldLabel>Email</FieldLabel>
  <FieldContent>
    <Input type="email" placeholder="Enter your email" />
    <FieldDescription>We'll never share your email.</FieldDescription>
  </FieldContent>
</Field>
```
