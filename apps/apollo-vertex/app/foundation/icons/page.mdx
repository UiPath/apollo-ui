import { IconGrid } from '@/app/_components/foundation/icon-grid';

# Icons

Lucide React is the icon library for Apollo Vertex — used across navigation, actions, status indicators, and form elements. Click any icon to copy its import statement.

**Package:** `lucide-react` · **Version:** `^0.555`

**Usage:**

```tsx
import { ChevronRight, AlertCircle, Check } from 'lucide-react';

<ChevronRight size={16} strokeWidth={1.5} />
```

<IconGrid />
