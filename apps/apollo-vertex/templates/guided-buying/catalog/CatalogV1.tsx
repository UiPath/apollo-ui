import { Selection } from "./v1/Selection";

/**
 * Catalog — Variant 1: the Guided Buying "Selection" screen (catalog goods).
 * Renders with a stubbed Intake request; see Selection's `request` prop seam.
 */
export function CatalogV1() {
  return <Selection />;
}
