"use client";

import { MyRequestsList } from "./MyRequestsList";

/**
 * My Requests — the list. Row clicks navigate to /requests/$id (RequestWindow).
 */
export function MyRequests() {
  return <MyRequestsList />;
}
