/**
 * Base Tailwind configuration for Apollo Wind
 * This will be populated with core config values in Phase 2
 */

export const baseConfig = {
  // Placeholder for base configuration
  // Will include: theme structure, breakpoints, etc.
};
