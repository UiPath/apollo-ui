// Promoted to ui/ so field-renderer's tooltip enabler and other consumers share one
// implementation; re-exported here to keep the guardrails-internal import paths stable.
export { InfoTooltip, type InfoTooltipProps } from '@/components/ui/info-tooltip';
