{
  "branches": ["main"],
  "tagFormat": "@uipath/apollo-core@${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/exec",
      {
        "verifyConditionsCmd": "echo 'Verify: @uipath/apollo-core'",
        "prepareCmd": "pnpm version ${nextRelease.version} --git-tag-version false --allow-same-version",
        "publishCmd": "pnpm publish --no-git-checks"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["package.json", "CHANGELOG.md"],
        "message": "chore(release): @uipath/apollo-core@${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    "@semantic-release/github"
  ]
}
