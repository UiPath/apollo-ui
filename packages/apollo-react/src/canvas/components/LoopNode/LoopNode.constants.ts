import { GRID_SPACING } from '../../constants';

export const DEFAULT_LOOP_NODE_TYPE = 'uipath.control-flow.foreach';
export const DEFAULT_LOOP_ICON = 'repeat';
export const DEFAULT_LOOP_TITLE = 'Loop';
export const DEFAULT_LOOP_HEADER_HEIGHT_PX = 40;

export const DEFAULT_LOOP_MIN_WIDTH = GRID_SPACING * 12;
export const DEFAULT_LOOP_MIN_HEIGHT = GRID_SPACING * 12;
export const LOOP_SHELL_RADIUS_PX = 20;
export const LOOP_FRAME_INSET_PX = 12;

export const DEFAULT_LOOP_SHELL_BACKGROUND = 'var(--canvas-background)';
export const DEFAULT_LOOP_FRAME_BACKGROUND = 'var(--canvas-background)';
export const DEFAULT_LOOP_FRAME_BORDER = 'var(--canvas-border)';
